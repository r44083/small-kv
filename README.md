# small-kv
Small key-value embedded database

## How to build:
* For Linux
```
cmake . -Bbuild
make -C build
```
* For Windows
```
cmake . -Bbuild -G "MinGW Makefiles"
make -C build
```

Find build binaries in ```build``` folder